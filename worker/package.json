{
  "name": "weipic-private-gallery-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "init:config": "node scripts/init-private-config.mjs",
    "build:config": "node scripts/build-gallery-kv.mjs",
    "check:config": "npm run build:config && node scripts/sync-gallery-kv.mjs",
    "sync:config": "npm run build:config && node scripts/sync-gallery-kv.mjs --apply",
    "test": "node --test test/worker.test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
