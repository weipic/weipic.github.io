{
  "name": "weipic-private-gallery-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "init:config": "node scripts/init-private-config.mjs",
    "build:config": "node scripts/build-gallery-kv.mjs",
    "check:config": "node scripts/cloud-source-notice.mjs",
    "sync:config": "node scripts/cloud-source-notice.mjs",
    "test": "node --test test/worker.test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
